import logging 

#logging.disable(logging.CRITICAL)  => Disable Logging 

logging.basicConfig(level=logging.DEBUG, format=' %(asctime)s - %(levelname)s - %(message)s') # Print Logging Info 

#logging.basicConfig(filename = 'log.txt', level=logging.DEBUG, ....) => Write File 



